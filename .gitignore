node_modules/
dist/
.env
*.env
# Aiken build artifacts
*/onchain/build/
*/onchain/plutus.json
**/onchain/build/
**/onchain/plutus.json
# Deploy state (env-specific, có thể chứa địa chỉ ví)
**/scripts/deployed.json
# State lượt diễn tập — mỗi mạng/mỗi lượt một bản, không phải nguồn sự thật chung.
# Provenance chính thức chép tay sang Genesis/offchain/src/deployed.ts.
**/scripts/canonical-state.json
**/scripts/canonical-v2-state.json

# mọi biến môi trường/seed
.env*

# npm lockfiles — tự sinh, phình diff/token khi review (npm install/ci tái tạo được)
**/package-lock.json

# Hạ tầng agent riêng — KHÔNG push lên org repo (tránh lộ nội dung nội bộ)
_Agents/
CLAUDE.md
.claude/

# macOS junk
.DS_Store
**/.DS_Store

# Vitest cache + runtime onboarding state
**/.vite/
**/scripts/onboarded-instances.json
